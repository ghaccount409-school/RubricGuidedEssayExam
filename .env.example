# Together.ai API key — https://api.together.xyz/
TOGETHER_API_KEY=

# Model id (see Together docs for current IDs)
TOGETHER_MODEL=meta-llama/Llama-3.3-70B-Instruct-Turbo

# Set to 1 to use fixed mock JSON (no API calls — good for UI / flow tests)
MOCK_LLM=0
